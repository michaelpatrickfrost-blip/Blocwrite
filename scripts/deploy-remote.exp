#!/usr/bin/expect -f
# Usage: DEPLOY_PASS="yourpass" ./scripts/deploy-remote.exp [command]
# Or: ./scripts/deploy-remote.exp "ls -la"
set timeout 60
set host "217.154.51.15"
set user "root"
set pass $env(DEPLOY_PASS)

if {$pass == ""} {
    puts "Error: set DEPLOY_PASS environment variable"
    exit 1
}

set cmd [lindex $argv 0]
if {$cmd == ""} { set cmd "hostname" }

spawn ssh -o StrictHostKeyChecking=no ${user}@${host} $cmd

expect {
    "password:" {
        send "$pass\r"
        exp_continue
    }
    "Password:" {
        send "$pass\r"
        exp_continue
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    eof { exit 0 }
    timeout { puts "Timeout"; exit 1 }
}
